{
  "name": "sleet_web4_hello",
  "private": true,
  "version": "0.0.0",
  "source": "src/index.html",
  "scripts": {
    "start": "parcel",
    "build": "npm run clean && parcel build",
    "clean": "rm -rf dist && rm -rf .parcel-cache",
    "web4_testnet": "web4-deploy dist sleethello.testnet --nearfs --yes",
    "web4_mainnet": "web4-deploy dist sleethello.near --nearfs --yes"
  },
  "devDependencies": {
    "parcel": "^2.14.0",
    "svgo": "^3.3.2",
    "web4-deploy": "^0.8.0"
  },
  "dependencies": {
    "web4-api-js": "^1.0.0"
  }
}
